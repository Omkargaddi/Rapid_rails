# docker-compose.yml overrides (copy to .env to customize)
POSTGRES_PASSWORD=rapidrails
JWT_SECRET=dev-jwt-secret-change-me
SMTP_HOST=smtp.gmail.com
SMTP_USER=
SMTP_PASS=
QUEUE_MAX_DEPTH=1000
JOB_TTL_SECONDS=600
CACHE_TTL_SECONDS=86400
WORKER_REPLICAS=2
NGINX_PORT=8080